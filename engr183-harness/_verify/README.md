Scratch-only. Not part of the shipped harness, not mounted into the WASM VFS.

`solved/` holds reference solutions for the three Unit 00 functions, used to prove the
harness reports 0/6 on the unsolved stubs in `../assignments/unit00/` and 6/6 once
solved. Run `run.m` from Octave with the repo root on the path (after `setup`).
